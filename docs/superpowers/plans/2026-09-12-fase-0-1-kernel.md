# Fases 0 y 1 — Saneamiento y kernel transversal · Plan de implementación

> **Para ejecutores:** los pasos usan checkbox (`- [ ]`). Ver `superpowers:executing-plans`.

**Goal:** dejar el backend en condiciones de soportar módulos ERP: transacciones
multi-agregado reales, concurrencia optimista, paginación, errores uniformes y SQL
endurecido.

**Architecture:** se introduce un `IDbSession` scoped que posee **una sola**
`NpgsqlConnection` compartida por EF Core y Dapper, de modo que una transacción abierta
sea visible para ambos. Sobre esa base se montan behaviors de MediatR (validación,
transacción, logging) y una convención de error única `Result`/`Result<T>`.

**Tech Stack:** .NET 10, EF Core 10 + Npgsql 10, Dapper 2.1.66, MediatR 13,
FluentValidation 11.11, xUnit 2.9.3, Moq 4.20.72.

**Spec:** [`docs/superpowers/specs/2026-09-12-axionerp-erp-modules-design.md`](../specs/2026-09-12-axionerp-erp-modules-design.md)

## Global Constraints

- TFM `net10.0`; nullable e implicit usings habilitados en todos los proyectos.
- Solución `test.slnx`. Build: `dotnet build test.slnx`. Test: `dotnet test test.slnx`.
- Paquetes con `PackageReference` directo por proyecto. **No** hay Central Package Management.
- Blazor es **Static SSR**: sin `@rendermode`, sin `@onclick`, sin handlers interactivos.
- Nombres de entidades, tablas y columnas en español, siguiendo la convención existente.
- Precisión decimal: cantidades `numeric(18,6)`, importes `numeric(18,4)`, porcentajes `numeric(9,5)`.
- **Commits locales SÍ, en la rama `feat/erp-fase-0-1-kernel`.** Cada tarea termina en
  build/test verde + commit local. **Prohibido** `git merge`, `git push` y tocar `main`
  (`CLAUDE.md`). Mensajes de commit convencionales (`feat:`, `fix:`, `chore:`,
  `refactor:`), sin líneas de atribución.
- **Docker no disponible en este entorno:** los tests de `tests/OpenSource1.SmokeTests/Api/`
  levantan `postgres:17-alpine` y no pueden ejecutarse. Se verifica con build + tests
  unitarios y se reporta explícitamente lo no ejecutado.

## Decisión tomada durante la planificación

`AppSettings` y `Entradas` **se conservan**. El spec dejaba abierto su destino; se decide
mantenerlos porque `README.md` los documenta como entregables del Entregable 1 ("CRUD
funcional de entradas", "CRUD funcional de configuraciones") y eliminarlos haría regresar
una entrega ya evaluada. Conservan su atributo `[Obsolete]` y sus 6 warnings CS0618.

---

## FASE 0 — Saneamiento urgente

### Task 0.1: Sacar los secretos del repositorio

**Files:**
- Create: `src/OpenSource1.Api/appsettings.Example.json`
- Modify: `src/OpenSource1.Api/appsettings.json` (valores reales → marcadores)
- Modify: `.gitignore:17-19`

- [ ] **Step 1: Crear la plantilla versionada**

`src/OpenSource1.Api/appsettings.Example.json` con la misma estructura pero sin secretos:
`ConnectionStrings` con `Password=__SET_IN_USER_SECRETS__`, `Jwt.SigningKey` con
`__SET_IN_USER_SECRETS__`.

- [ ] **Step 2: Neutralizar `appsettings.json`**

Sustituir en `appsettings.json:3-4` las contraseñas por `__SET_IN_USER_SECRETS__` y en
`:9` la clave de firma por `__SET_IN_USER_SECRETS__`.

- [ ] **Step 3: Ignorar el archivo**

Añadir a `.gitignore` bajo la sección `# ASP.NET Core`:

```gitignore
appsettings.json
src/OpenSource1.Api/appsettings.json
```

- [ ] **Step 4: Dejar de trackearlo sin borrarlo del disco**

```bash
git rm --cached src/OpenSource1.Api/appsettings.json
```

- [ ] **Step 5: Verificar**

```bash
git ls-files | grep -c 'src/OpenSource1.Api/appsettings.json'   # esperado: 0
```

**Nota para el usuario:** las credenciales expuestas siguen en el historial de git. Hay
que **rotarlas** (contraseña de PostgreSQL y clave JWT); quitarlas del índice no las borra
del historial.

### Task 0.2: Resolver la vulnerabilidad alta de OpenApi

**Files:**
- Modify: `src/OpenSource1.Api/OpenSource1.Api.csproj:11`

- [ ] **Step 1: Comprobar el warning actual**

```bash
dotnet build test.slnx 2>&1 | grep NU1903
```
Esperado: warning sobre `Microsoft.OpenApi 2.0.0` (GHSA-v5pm-xwqc-g5wc).

- [ ] **Step 2: Fijar la versión parcheada de la transitiva**

Añadir al `ItemGroup` de `OpenSource1.Api.csproj` una referencia directa que gane a la
transitiva:

```xml
<PackageReference Include="Microsoft.OpenApi" Version="2.1.0" />
```

Si 2.1.0 no resuelve, usar la versión mínima no vulnerable que `dotnet list package
--vulnerable --include-transitive` reporte como segura.

- [ ] **Step 3: Verificar que el warning desaparece**

```bash
dotnet build test.slnx 2>&1 | grep -c NU1903   # esperado: 0
```

### Task 0.3: Desactivar el borrado automático de la base

**Files:**
- Modify: `src/OpenSource1.Api/appsettings.json:22`
- Modify: `src/OpenSource1.Api/appsettings.Example.json`

- [ ] **Step 1: Cambiar el flag**

`"RecreateOnPendingModelChangesInDevelopment": false`

Motivo: con `true`, `DatabaseMigrationExecutor.cs:25-32` ejecuta `EnsureDeletedAsync()` +
`EnsureCreatedAsync()` ante un `PendingModelChangesWarning`, borrando la base entera. Las
fases 2-6 iteran el modelo constantemente.

- [ ] **Step 2: Verificar**

```bash
grep -n RecreateOnPendingModelChangesInDevelopment src/OpenSource1.Api/appsettings.json
```

### Task 0.4: Cerrar el fallback permisivo de CORS

**Files:**
- Modify: `src/OpenSource1.Api/Program.cs:45-64`

- [ ] **Step 1: Sustituir el `else` permisivo por un fallo explícito**

```csharp
builder.Services.AddCors(options =>
{
    var corsOptions = builder.Configuration.GetSection(CorsOptions.SectionName).Get<CorsOptions>() ?? new CorsOptions();

    if (corsOptions.AllowedOrigins.Length == 0)
    {
        throw new InvalidOperationException(
            "La sección 'Cors:AllowedOrigins' está vacía. Configure los orígenes permitidos; " +
            "no se permite el fallback a AllowAnyOrigin.");
    }

    options.AddPolicy(CorsOptions.PolicyName, policy =>
        policy.WithOrigins(corsOptions.AllowedOrigins)
            .AllowAnyHeader()
            .AllowAnyMethod());
});
```

- [ ] **Step 2: Verificar build**

```bash
dotnet build test.slnx
```

### Task 0.5: Corregir el header `Location` al crear usuario

**Files:**
- Modify: `src/OpenSource1.Api/Controllers/UsersController.cs:46`

- [ ] **Step 1: Pasar el Id, no el email**

`CreatedAtAction(nameof(GetById), new { userId = request.Email }, ...)` genera un
`Location` que devuelve 404. Cambiar a usar el identificador real del usuario creado
(el que devuelve `IUserAdminService.CreateUserAsync`). Si el servicio no lo devuelve,
ampliar su contrato para que lo haga.

- [ ] **Step 2: Verificar build**

```bash
dotnet build test.slnx
```

---

## FASE 1 — Kernel transversal

### Task 1.1: Tipos `Error`, `Result` y `Result<T>`

**Files:**
- Create: `src/OpenSource1.Core/Common/Error.cs`
- Create: `src/OpenSource1.Core/Common/Result.cs`
- Test: `tests/OpenSource1.SmokeTests/Core/Common/ResultTests.cs`

**Interfaces:**
- Produces: `Error(string Codigo, string Mensaje, string? Campo)`;
  `Result.Exito()`, `Result.Fallo(params Error[])`, `Result.EsExito`, `Result.Errores`;
  `Result<T>.Exito(T)`, `Result<T>.Fallo(params Error[])`, `Result<T>.Valor`.
  Todas las tareas posteriores y todas las fases siguientes consumen estos tipos.

- [ ] **Step 1: Escribir el test que falla**

```csharp
using OpenSource1.Core.Common;

namespace OpenSource1.SmokeTests.Core.Common;

public class ResultTests
{
    [Fact]
    public void Exito_NoTieneErrores()
    {
        var resultado = Result.Exito();

        Assert.True(resultado.EsExito);
        Assert.Empty(resultado.Errores);
    }

    [Fact]
    public void Fallo_ConservaLosErrores()
    {
        var resultado = Result.Fallo(new Error("producto.no_encontrado", "No existe.", "Id"));

        Assert.False(resultado.EsExito);
        var error = Assert.Single(resultado.Errores);
        Assert.Equal("producto.no_encontrado", error.Codigo);
        Assert.Equal("Id", error.Campo);
    }

    [Fact]
    public void ResultGenerico_ExitoExponeElValor()
    {
        var resultado = Result<int>.Exito(42);

        Assert.True(resultado.EsExito);
        Assert.Equal(42, resultado.Valor);
    }

    [Fact]
    public void ResultGenerico_FalloNoTieneValor()
    {
        var resultado = Result<int>.Fallo(new Error("x", "y"));

        Assert.False(resultado.EsExito);
        Assert.Equal(default, resultado.Valor);
    }

    [Fact]
    public void Fallo_SinErrores_Lanza()
    {
        Assert.Throws<ArgumentException>(() => Result.Fallo());
    }
}
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
dotnet test test.slnx --filter "FullyQualifiedName~ResultTests"
```
Esperado: error de compilación, `Result` no existe.

- [ ] **Step 3: Implementar**

`src/OpenSource1.Core/Common/Error.cs`:

```csharp
namespace OpenSource1.Core.Common;

/// <summary>Error de negocio con código estable para que la API y la UI lo traduzcan.</summary>
public readonly record struct Error(string Codigo, string Mensaje, string? Campo = null);
```

`src/OpenSource1.Core/Common/Result.cs`:

```csharp
namespace OpenSource1.Core.Common;

public class Result
{
    protected Result(bool esExito, IReadOnlyList<Error> errores)
    {
        EsExito = esExito;
        Errores = errores;
    }

    public bool EsExito { get; }
    public bool EsFallo => !EsExito;
    public IReadOnlyList<Error> Errores { get; }

    public static Result Exito() => new(true, []);

    public static Result Fallo(params Error[] errores)
    {
        if (errores is null || errores.Length == 0)
        {
            throw new ArgumentException("Un resultado fallido requiere al menos un error.", nameof(errores));
        }

        return new Result(false, errores);
    }
}

public sealed class Result<T> : Result
{
    private Result(bool esExito, T? valor, IReadOnlyList<Error> errores)
        : base(esExito, errores) => Valor = valor;

    public T? Valor { get; }

    public static Result<T> Exito(T valor) => new(true, valor, []);

    public static new Result<T> Fallo(params Error[] errores)
    {
        if (errores is null || errores.Length == 0)
        {
            throw new ArgumentException("Un resultado fallido requiere al menos un error.", nameof(errores));
        }

        return new Result<T>(false, default, errores);
    }
}
```

- [ ] **Step 4: Ejecutar y ver que pasa**

```bash
dotnet test test.slnx --filter "FullyQualifiedName~ResultTests"
```
Esperado: 5 passed.

### Task 1.2: Corregir `ValueObject.Equals`

**Files:**
- Modify: `src/OpenSource1.Core/Abstractions/ValueObject.cs:7-11`
- Test: `tests/OpenSource1.SmokeTests/Core/Abstractions/ValueObjectTests.cs`

- [ ] **Step 1: Escribir el test que falla**

```csharp
using OpenSource1.Core.Abstractions;

namespace OpenSource1.SmokeTests.Core.Abstractions;

public class ValueObjectTests
{
    private sealed class Codigo(string valor) : ValueObject
    {
        public string Valor { get; } = valor;
        protected override IEnumerable<object?> GetEqualityComponents() { yield return Valor; }
    }

    private sealed class Nombre(string valor) : ValueObject
    {
        public string Valor { get; } = valor;
        protected override IEnumerable<object?> GetEqualityComponents() { yield return Valor; }
    }

    [Fact]
    public void TiposDistintos_ConMismoComponente_NoSonIguales()
    {
        Assert.NotEqual<object>(new Codigo("KG"), new Nombre("KG"));
    }

    [Fact]
    public void MismoTipo_ConMismoComponente_SonIguales()
    {
        Assert.Equal(new Codigo("KG"), new Codigo("KG"));
    }

    [Fact]
    public void OperadorIgualdad_FuncionaConNulos()
    {
        Codigo? nulo = null;
        Assert.True(nulo == null);
        Assert.False(new Codigo("KG") == null);
    }
}
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
dotnet test test.slnx --filter "FullyQualifiedName~ValueObjectTests"
```
Esperado: `TiposDistintos_ConMismoComponente_NoSonIguales` FAILS (hoy son iguales) y
`OperadorIgualdad_FuncionaConNulos` no compila (no hay operadores).

- [ ] **Step 3: Implementar**

```csharp
namespace OpenSource1.Core.Abstractions;

public abstract class ValueObject
{
    protected abstract IEnumerable<object?> GetEqualityComponents();

    public override bool Equals(object? obj) =>
        obj is ValueObject other
        && other.GetType() == GetType()
        && GetEqualityComponents().SequenceEqual(other.GetEqualityComponents());

    public override int GetHashCode() =>
        GetEqualityComponents().Aggregate(GetType().GetHashCode(), HashCode.Combine);

    public static bool operator ==(ValueObject? left, ValueObject? right) =>
        left is null ? right is null : left.Equals(right);

    public static bool operator !=(ValueObject? left, ValueObject? right) => !(left == right);
}
```

Nota: el `GetHashCode` ahora siembra con el tipo, de modo que sea coherente con `Equals`.

- [ ] **Step 4: Ejecutar y ver que pasa**

```bash
dotnet test test.slnx --filter "FullyQualifiedName~ValueObjectTests"
dotnet build test.slnx
```
Esperado: 3 passed, build verde (los VOs existentes siguen compilando).

### Task 1.3: Allow-list de columnas y corrección de filtros exactos

**Files:**
- Create: `src/OpenSource1.Infrastructure/Data/Queries/ColumnasPermitidas.cs`
- Modify: `src/OpenSource1.Infrastructure/Data/Queries/FilterExpressionBuilder.cs`
- Test: `tests/OpenSource1.SmokeTests/Infrastructure/Queries/ColumnasPermitidasTests.cs`
- Test: `tests/OpenSource1.SmokeTests/Infrastructure/Queries/FilterExpressionBuilderTests.cs`

**Interfaces:**
- Produces: `ColumnasPermitidas(params string[] columnas)` con `bool EsValida(string)` y
  `string Citar(string)`; `FilterExpressionBuilder` pasa a ser `public` y sus dos métodos
  reciben `ColumnasPermitidas`. `AddExactFilter` devuelve `Result`.

- [ ] **Step 1: Escribir los tests que fallan**

`ColumnasPermitidasTests.cs`:

```csharp
using OpenSource1.Infrastructure.Data.Queries;

namespace OpenSource1.SmokeTests.Infrastructure.Queries;

public class ColumnasPermitidasTests
{
    private static readonly ColumnasPermitidas Permitidas = new("Nombre", "Email");

    [Fact]
    public void EsValida_DistingueColumnasConocidas()
    {
        Assert.True(Permitidas.EsValida("Nombre"));
        Assert.False(Permitidas.EsValida("Contrasena"));
    }

    [Fact]
    public void Citar_DevuelveLaColumnaEntreComillas()
    {
        Assert.Equal("\"Nombre\"", Permitidas.Citar("Nombre"));
    }

    [Theory]
    [InlineData("Nombre\"; DROP TABLE \"Clientes")]
    [InlineData("1=1")]
    [InlineData("")]
    public void Citar_RechazaColumnasNoPermitidas(string columna)
    {
        Assert.Throws<ArgumentException>(() => Permitidas.Citar(columna));
    }
}
```

`FilterExpressionBuilderTests.cs`:

```csharp
using Dapper;
using OpenSource1.Infrastructure.Data.Queries;

namespace OpenSource1.SmokeTests.Infrastructure.Queries;

public class FilterExpressionBuilderTests
{
    private static readonly ColumnasPermitidas Permitidas = new("Nombre", "Precio");

    [Fact]
    public void AddTextFilter_ColumnaNoPermitida_Lanza()
    {
        var filtros = new List<string>();
        var parametros = new DynamicParameters();

        Assert.Throws<ArgumentException>(() =>
            FilterExpressionBuilder.AddTextFilter(filtros, parametros, Permitidas, "Inyeccion", "x"));
    }

    [Fact]
    public void AddExactFilter_ValorInvalido_DevuelveFallo()
    {
        var filtros = new List<string>();
        var parametros = new DynamicParameters();

        var resultado = FilterExpressionBuilder.AddExactFilter<decimal>(
            filtros, parametros, Permitidas, "Precio", "abc",
            t => (decimal.TryParse(t, out var v), v));

        Assert.False(resultado.EsExito);
        Assert.Equal("filtro.valor_invalido", resultado.Errores[0].Codigo);
        Assert.Empty(filtros);
    }

    [Fact]
    public void AddExactFilter_ValorValido_AgregaClausula()
    {
        var filtros = new List<string>();
        var parametros = new DynamicParameters();

        var resultado = FilterExpressionBuilder.AddExactFilter<decimal>(
            filtros, parametros, Permitidas, "Precio", "10.50",
            t => (decimal.TryParse(t, System.Globalization.CultureInfo.InvariantCulture, out var v), v));

        Assert.True(resultado.EsExito);
        Assert.Single(filtros);
    }
}
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
dotnet test test.slnx --filter "FullyQualifiedName~Queries"
```
Esperado: no compila (`ColumnasPermitidas` no existe, `FilterExpressionBuilder` es
`internal` y su firma no lleva allow-list).

- [ ] **Step 3: Implementar `ColumnasPermitidas`**

```csharp
using System.Collections.Frozen;

namespace OpenSource1.Infrastructure.Data.Queries;

/// <summary>
/// Lista blanca de columnas que pueden interpolarse en SQL. Cierra el sink de inyección
/// que existía al construir cláusulas con el nombre de columna sin validar.
/// </summary>
public sealed class ColumnasPermitidas
{
    private readonly FrozenSet<string> _columnas;

    public ColumnasPermitidas(params string[] columnas) =>
        _columnas = columnas.ToFrozenSet(StringComparer.Ordinal);

    public bool EsValida(string? columna) =>
        !string.IsNullOrWhiteSpace(columna) && _columnas.Contains(columna);

    public string Citar(string columna)
    {
        if (!EsValida(columna))
        {
            throw new ArgumentException($"Columna no permitida: '{columna}'.", nameof(columna));
        }

        return $"\"{columna}\"";
    }
}
```

- [ ] **Step 4: Endurecer `FilterExpressionBuilder`**

Cambiar `internal static class` por `public static class`, añadir el parámetro
`ColumnasPermitidas permitidas` a ambos métodos y sustituir la interpolación cruda:

```csharp
// AddTextFilter, antes línea 28
orClauses.Add($"{permitidas.Citar(column)} ILIKE @{paramName}");

// AddExactFilter, antes línea 62
orClauses.Add($"{permitidas.Citar(column)} = @{paramName}");
```

`AddExactFilter` pasa a devolver `Result`: si `tryParse` falla para algún término,
devuelve `Result.Fallo(new Error("filtro.valor_invalido", $"El valor '{term}' no es válido
para el filtro '{column}'.", column))` **sin** añadir cláusulas. Si todo parsea, añade la
cláusula y devuelve `Result.Exito()`.

`AddTextFilter` sigue siendo `void` (no puede fallar por valor).

- [ ] **Step 5: Actualizar los llamantes — cambio MÍNIMO para compilar**

`DapperClienteReadRepository.cs:31-37` y `DapperProductoReadRepository.cs:31-40` declaran
su `ColumnasPermitidas` estática con las columnas que ya usan y pasan esa instancia a los
dos métodos del builder.

**Alcance acotado (Ruling 2):** en esta tarea NO se cambia la firma de los repositorios ni
se propaga el `Result` hacia arriba. Cuando `AddExactFilter` devuelve fallo, el repositorio
lanza `ArgumentException` con el mensaje del error. La reescritura real de los repositorios
—`IDbSession`, paginación y propagación de `Result`— ocurre una sola vez, en las Tasks 1.5
y 1.5b. Reescribir los mismos métodos tres veces es desperdicio puro.

- [ ] **Step 6: Ejecutar y ver que pasa**

```bash
dotnet test test.slnx --filter "FullyQualifiedName~Queries"
dotnet build test.slnx
```

### Task 1.4: Contrato de paginación

**Files:**
- Create: `src/OpenSource1.Core/Common/PageRequest.cs`
- Create: `src/OpenSource1.Core/Common/PagedResult.cs`
- Test: `tests/OpenSource1.SmokeTests/Core/Common/PageRequestTests.cs`

**Interfaces:**
- Produces: `PageRequest(int Pagina, int TamanoPagina, string? OrdenarPor, bool Descendente)`
  con `Normalizar()`, `int Offset`; `PagedResult<T>(IReadOnlyList<T> Items, int Pagina, int TamanoPagina, long Total)`
  con `int TotalPaginas`. Los consumen todos los repositorios de lectura de todas las fases.

- [ ] **Step 1: Escribir el test que falla**

```csharp
using OpenSource1.Core.Common;

namespace OpenSource1.SmokeTests.Core.Common;

public class PageRequestTests
{
    [Fact]
    public void Normalizar_CorrigePaginaMenorQueUno()
    {
        Assert.Equal(1, new PageRequest(0, 50).Normalizar().Pagina);
        Assert.Equal(1, new PageRequest(-5, 50).Normalizar().Pagina);
    }

    [Fact]
    public void Normalizar_LimitaElTamanoMaximo()
    {
        Assert.Equal(PageRequest.TamanoMaximo, new PageRequest(1, 5_000).Normalizar().TamanoPagina);
    }

    [Fact]
    public void Normalizar_CorrigeTamanoMenorQueUno()
    {
        Assert.Equal(PageRequest.TamanoPorDefecto, new PageRequest(1, 0).Normalizar().TamanoPagina);
    }

    [Fact]
    public void Offset_SeCalculaDesdeLaPaginaNormalizada()
    {
        Assert.Equal(0, new PageRequest(1, 20).Normalizar().Offset);
        Assert.Equal(40, new PageRequest(3, 20).Normalizar().Offset);
    }

    [Fact]
    public void TotalPaginas_RedondeaHaciaArriba()
    {
        var pagina = new PagedResult<int>([1, 2], 1, 20, 41);
        Assert.Equal(3, pagina.TotalPaginas);
    }

    [Fact]
    public void TotalPaginas_ConCeroElementos_EsCero()
    {
        Assert.Equal(0, new PagedResult<int>([], 1, 20, 0).TotalPaginas);
    }
}
```

- [ ] **Step 2: Ejecutar y ver que falla**

```bash
dotnet test test.slnx --filter "FullyQualifiedName~PageRequestTests"
```

- [ ] **Step 3: Implementar**

```csharp
namespace OpenSource1.Core.Common;

public sealed record PageRequest(
    int Pagina = 1,
    int TamanoPagina = PageRequest.TamanoPorDefecto,
    string? OrdenarPor = null,
    bool Descendente = true)
{
    public const int TamanoPorDefecto = 50;
    public const int TamanoMaximo = 200;

    public PageRequest Normalizar() => this with
    {
        Pagina = Pagina < 1 ? 1 : Pagina,
        TamanoPagina = TamanoPagina switch
        {
            < 1 => TamanoPorDefecto,
            > TamanoMaximo => TamanoMaximo,
            _ => TamanoPagina,
        },
    };

    public int Offset => (Pagina - 1) * TamanoPagina;
}
```

```csharp
namespace OpenSource1.Core.Common;

public sealed record PagedResult<T>(IReadOnlyList<T> Items, int Pagina, int TamanoPagina, long Total)
{
    public int TotalPaginas => TamanoPagina <= 0 ? 0 : (int)Math.Ceiling(Total / (double)TamanoPagina);

    public static PagedResult<T> Vacio(PageRequest peticion) =>
        new([], peticion.Pagina, peticion.TamanoPagina, 0);
}
```

- [ ] **Step 4: Ejecutar y ver que pasa**

```bash
dotnet test test.slnx --filter "FullyQualifiedName~PageRequestTests"
```

### Task 1.5: `IDbSession` — conexión compartida entre EF y Dapper

Es la pieza central de la fase. Sin esto no hay transacciones multi-agregado reales.

**Files:**
- Create: `src/OpenSource1.Application/Data/IDbSession.cs`
- Create: `src/OpenSource1.Infrastructure/Data/DbSession.cs`
- Modify: `src/OpenSource1.Infrastructure/Data/DependencyInjection.cs:36-42`
- Modify: los 4 repositorios de `src/OpenSource1.Infrastructure/Data/Queries/`
- Delete: `src/OpenSource1.Application/Data/IDbConnectionFactory.cs` y `src/OpenSource1.Infrastructure/Data/NpgsqlConnectionFactory.cs`
- Test: `tests/OpenSource1.SmokeTests/Infrastructure/DbSessionTests.cs`

**Interfaces:**
- Produces: `IDbSession` con `DbConnection Connection`, `DbTransaction? CurrentTransaction`,
  `Task<IAsyncDisposable> BeginTransactionAsync(CancellationToken)`,
  `Task CommitAsync(CancellationToken)`, `Task RollbackAsync(CancellationToken)`.
  Lo consumen los repositorios Dapper y el `TransactionBehavior` de la Task 1.7.

- [ ] **Step 1: Definir el contrato**

```csharp
using System.Data.Common;

namespace OpenSource1.Application.Data;

/// <summary>
/// Posee la única conexión física del scope. EF Core y Dapper la comparten, de modo que
/// una transacción abierta aquí es visible para ambos.
/// </summary>
public interface IDbSession : IAsyncDisposable
{
    DbConnection Connection { get; }
    DbTransaction? CurrentTransaction { get; }
    bool HayTransaccionActiva { get; }

    Task<IAsyncDisposable> BeginTransactionAsync(CancellationToken cancellationToken = default);
    Task CommitAsync(CancellationToken cancellationToken = default);
    Task RollbackAsync(CancellationToken cancellationToken = default);
    Task EnsureOpenAsync(CancellationToken cancellationToken = default);
}
```

- [ ] **Step 2: Implementar `DbSession`**

```csharp
using System.Data;
using System.Data.Common;
using Microsoft.Extensions.Configuration;
using Npgsql;
using OpenSource1.Application.Data;

namespace OpenSource1.Infrastructure.Data;

public sealed class DbSession : IDbSession
{
    private readonly NpgsqlConnection _connection;
    private NpgsqlTransaction? _transaction;
    private bool _disposed;

    public DbSession(IConfiguration configuration)
    {
        var connectionString = configuration.GetConnectionString("DefaultConnection")
            ?? throw new InvalidOperationException("Connection string 'DefaultConnection' was not found.");

        // Se crea sin abrir: EF Core la abre cuando la necesita y Dapper reutiliza la misma instancia.
        _connection = new NpgsqlConnection(connectionString);
    }

    public DbConnection Connection => _connection;
    public DbTransaction? CurrentTransaction => _transaction;
    public bool HayTransaccionActiva => _transaction is not null;

    public async Task EnsureOpenAsync(CancellationToken cancellationToken = default)
    {
        if (_connection.State != ConnectionState.Open)
        {
            await _connection.OpenAsync(cancellationToken);
        }
    }

    public async Task<IAsyncDisposable> BeginTransactionAsync(CancellationToken cancellationToken = default)
    {
        if (_transaction is not null)
        {
            // Transacción anidada: se une a la existente y no hace commit al salir.
            return new AmbitoAnidado();
        }

        await EnsureOpenAsync(cancellationToken);
        _transaction = await _connection.BeginTransactionAsync(cancellationToken);
        return new AmbitoRaiz(this);
    }

    public async Task CommitAsync(CancellationToken cancellationToken = default)
    {
        if (_transaction is null)
        {
            return;
        }

        await _transaction.CommitAsync(cancellationToken);
        await _transaction.DisposeAsync();
        _transaction = null;
    }

    public async Task RollbackAsync(CancellationToken cancellationToken = default)
    {
        if (_transaction is null)
        {
            return;
        }

        await _transaction.RollbackAsync(cancellationToken);
        await _transaction.DisposeAsync();
        _transaction = null;
    }

    public async ValueTask DisposeAsync()
    {
        if (_disposed)
        {
            return;
        }

        _disposed = true;

        if (_transaction is not null)
        {
            await _transaction.RollbackAsync();
            await _transaction.DisposeAsync();
            _transaction = null;
        }

        await _connection.DisposeAsync();
    }

    private sealed class AmbitoAnidado : IAsyncDisposable
    {
        public ValueTask DisposeAsync() => ValueTask.CompletedTask;
    }

    private sealed class AmbitoRaiz(DbSession sesion) : IAsyncDisposable
    {
        public async ValueTask DisposeAsync()
        {
            // Si nadie hizo commit, deshacer. Protege contra excepciones a medio camino.
            await sesion.RollbackAsync();
        }
    }
}
```

- [ ] **Step 3: Cablear EF sobre la misma conexión**

En `DependencyInjection.cs`, sustituir las líneas 36-42:

```csharp
services.AddScoped<DbSession>();
services.AddScoped<IDbSession>(sp => sp.GetRequiredService<DbSession>());

services.AddDbContext<ApplicationDbContext>((sp, options) =>
    options.UseNpgsql(sp.GetRequiredService<DbSession>().Connection));

// La base de Identity mantiene su propia conexión: es otra base de datos.
services.AddDbContext<AppIdentityDbContext>(options =>
    options.UseNpgsql(identityConnectionString));
```

Eliminar el registro de `IDbConnectionFactory` y borrar las dos clases del factory.

- [ ] **Step 4: Migrar los repositorios Dapper**

Los 4 repositorios (`DapperAppSettingReadRepository`, `DapperEntradaReadRepository`,
`DapperClienteReadRepository`, `DapperProductoReadRepository`) pasan de
`IDbConnectionFactory` a `IDbSession`. El patrón cambia de:

```csharp
using var connection = await connectionFactory.CreateOpenConnectionAsync(cancellationToken);
return await connection.QueryAsync<T>(sql, parameters);
```

a:

```csharp
await session.EnsureOpenAsync(cancellationToken);
return await session.Connection.QueryAsync<T>(
    new CommandDefinition(sql, parameters, session.CurrentTransaction, cancellationToken: cancellationToken));
```

La conexión ya **no** se dispone en el método: pertenece al scope. Y pasar
`session.CurrentTransaction` es lo que hace que la lectura vea la escritura en curso.

- [ ] **Step 5: Escribir el test de la propiedad crítica**

```csharp
// tests/OpenSource1.SmokeTests/Infrastructure/DbSessionTests.cs
// REQUIERE DOCKER: usa PostgresTestFixture. Si el daemon no está, el test se omite.
```

El test que importa: dentro de una transacción, escribir con EF y leer con Dapper **debe
ver** la escritura; tras el rollback, **no debe verla**. Es la propiedad que hoy no se
cumple y toda la Fase 4 y 6 dependen de ella.

```csharp
[Fact]
public async Task LecturaDapper_VeEscrituraEfDentroDeLaMismaTransaccion()
{
    await using var scope = _factory.Services.CreateAsyncScope();
    var session = scope.ServiceProvider.GetRequiredService<IDbSession>();
    var dbContext = scope.ServiceProvider.GetRequiredService<ApplicationDbContext>();

    await using var tx = await session.BeginTransactionAsync();
    await dbContext.Database.UseTransactionAsync(session.CurrentTransaction);

    var entrada = new Entrada { Titulo = "T", Tipo = "X", Estado = "A" };
    dbContext.Entradas.Add(entrada);
    await dbContext.SaveChangesAsync();

    var encontrado = await session.Connection.QuerySingleOrDefaultAsync<Guid?>(
        new CommandDefinition(
            "SELECT \"Id\" FROM \"Entradas\" WHERE \"Id\" = @Id",
            new { entrada.Id }, session.CurrentTransaction));

    Assert.Equal(entrada.Id, encontrado);   // hoy fallaría: conexiones distintas

    await session.RollbackAsync();
}
```

- [ ] **Step 6: Verificar**

```bash
dotnet build test.slnx
dotnet test test.slnx --filter "FullyQualifiedName~DbSessionTests"
```
Si Docker no está disponible, registrar el test como **no ejecutado**, no como verde.

### Task 1.5b: Propagar el contrato de paginación por todas las capas

Añadida durante la planificación (Ruling 3): el plan original daba por hecho este trabajo
en la Task 1.4, que solo crea los tipos. Sin esta tarea ningún listado queda paginado y la
Fase 1 no cumple el spec.

**Files:**
- Modify: `src/OpenSource1.Application/Features/Clientes/IClienteReadRepository.cs`
- Modify: `src/OpenSource1.Application/Features/Productos/IProductoReadRepository.cs`
- Modify: `src/OpenSource1.Application/Features/Entradas/IEntradaReadRepository.cs`
- Modify: `src/OpenSource1.Application/Features/AppSettings/IAppSettingReadRepository.cs`
- Modify: los 4 repositorios de `src/OpenSource1.Infrastructure/Data/Queries/`
- Modify: `ListClientesQuery` / `ListProductosQuery` / `ListEntradasQuery` / `ListAppSettingsQuery` y sus handlers
- Modify: `ClientesController.cs`, `ProductosController.cs`, `EntradasController.cs`, `AppSettingsController.cs`
- Modify: `src/OpenSource1.Blazor/Services/*ApiClient.cs` y las páginas de listado que los consumen
- Modify: `src/OpenSource1.Application/Data/Repositories/IGenericRepository.cs` y `GenericRepository.cs`

**Interfaces:**
- Consumes: `PageRequest`, `PagedResult<T>` (Task 1.4); `Result<T>` (Task 1.1);
  `ColumnasPermitidas` (Task 1.3); `IDbSession` (Task 1.5).
- Produces: `Task<Result<PagedResult<TResponse>>> ListAsync(<Criteria>, PageRequest, CancellationToken)`
  en los 4 repositorios de lectura. Lo consumen todas las vistas de la Fase 7.

- [ ] **Step 1: Cambiar las firmas de los 4 contratos de lectura**

```csharp
Task<Result<PagedResult<ClienteResponse>>> ListAsync(
    ClienteSearchCriteria criterios,
    PageRequest paginacion,
    CancellationToken cancellationToken = default);
```

- [ ] **Step 2: Implementar la paginación en SQL**

Dos consultas sobre la misma conexión y transacción: el total y la página.

```sql
-- total
SELECT COUNT(*) FROM "Clientes" WHERE "IsDeleted" = false {filtros};

-- página
SELECT {columnas} FROM "Clientes"
WHERE "IsDeleted" = false {filtros}
ORDER BY {columnaOrden} {direccion}
LIMIT @TamanoPagina OFFSET @Offset;
```

`{columnaOrden}` sale **siempre** de `ColumnasPermitidas.Citar(...)`; si
`PageRequest.OrdenarPor` es nulo o no está permitido, se usa `"CreatedAtUtc"` descendente.
`{direccion}` es el literal `ASC` o `DESC` elegido por `bool Descendente`, nunca texto del
usuario.

Si `AddExactFilter` devuelve fallo, el método devuelve ese `Result` sin ejecutar SQL.

- [ ] **Step 3: Añadir paginación a `IGenericRepository`**

```csharp
Task<PagedResult<TEntity>> ListPagedAsync(
    Expression<Func<TEntity, bool>>? predicate,
    PageRequest paginacion,
    CancellationToken cancellationToken = default);
```

Implementado con `CountAsync` + `Skip(peticion.Offset).Take(peticion.TamanoPagina)`.
`ListAsync` sin paginar **se conserva** solo para uso interno de los handlers de escritura.

- [ ] **Step 4: Propagar por queries y handlers**

`ListClientesQuery` gana `PageRequest Paginacion` y pasa a implementar
`IQuery<Result<PagedResult<ClienteResponse>>>`. Su handler delega al repositorio. Igual
para los otros tres.

- [ ] **Step 5: Propagar por los controllers**

Los endpoints de listado aceptan `[FromQuery] int pagina = 1`,
`[FromQuery] int tamanoPagina = 50`, `[FromQuery] string? ordenarPor = null`,
`[FromQuery] bool descendente = true`, construyen el `PageRequest`, y devuelven el
`PagedResult<T>` usando `ResultExtensions.ToActionResult` (Task 1.8). Anotar con
`[ProducesResponseType<PagedResult<ClienteResponse>>(200)]`.

- [ ] **Step 6: Propagar por Blazor — Static SSR**

Los `*ApiClient` devuelven `PagedResult<T>`. Las páginas de listado
(`Clientes.razor`, `Productos.razor`, `Entradas.razor`, `AppSettings.razor`) reciben la
página por `[SupplyParameterFromQuery]` y renderizan enlaces de paginación como
`<a href="...?pagina=N">`.

**Restricción dura:** sin `@rendermode`, sin `@onclick`, sin handlers interactivos. La
navegación entre páginas es por enlace GET, no por evento.

- [ ] **Step 7: Actualizar los tests existentes**

Los tests de API que hoy esperan un array JSON plano
(`ClientesApiTests.cs`, `ProductosApiTests.cs`, `AppSettingsApiTests.cs`) pasan a
deserializar `PagedResult<T>` y a leer `.Items`.

- [ ] **Step 8: Test de paginación**

```csharp
[Fact]
public async Task ListAsync_RespetaElTamanoDePaginaYDevuelveElTotal()
{
    // 3 clientes en la base, TamanoPagina = 2
    var resultado = await _repositorio.ListAsync(new ClienteSearchCriteria(), new PageRequest(1, 2));

    Assert.True(resultado.EsExito);
    Assert.Equal(2, resultado.Valor!.Items.Count);
    Assert.Equal(3, resultado.Valor.Total);
    Assert.Equal(2, resultado.Valor.TotalPaginas);
}

[Fact]
public async Task ListAsync_ColumnaDeOrdenNoPermitida_CaeAlOrdenPorDefecto()
{
    var resultado = await _repositorio.ListAsync(
        new ClienteSearchCriteria(), new PageRequest(1, 10, OrdenarPor: "\"; DROP TABLE \"Clientes"));

    Assert.True(resultado.EsExito);   // no lanza, no inyecta: cae a CreatedAtUtc
}
```

- [ ] **Step 9: Verificar**

```bash
dotnet build test.slnx
dotnet test test.slnx --filter "FullyQualifiedName!~Api"
```
Build verde y unitarios verdes. Los tests de API requieren Docker: reportar como no
ejecutados si no está disponible.

- [ ] **Step 10: Commit**

```bash
git add -A && git commit -m "feat: propagar contrato de paginacion por Application, API y Blazor"
```

### Task 1.5c: HOTFIX — restaurar la construcción del modelo EF Core

**Inserción de emergencia, no prevista en el plan original.** El commit `6b4392a` (fix de
`nombreCampo` en value objects, Task 1.8 ronda 2) añadió parámetros de constructor
públicos que EF Core no puede enlazar a ninguna propiedad mapeada. Resultado, confirmado
por reproducción directa sin base de datos: **`ApplicationDbContext` no puede construir su
modelo hoy**, con `InvalidOperationException: No suitable constructor was found`. Como los
cuatro `appsettings*.json` tienen `ApplyMigrationsOnStartup: true`, esto significa que **la
API no arranca** contra una base de datos real.

Afecta a `DireccionCliente` (constructor de 3 parámetros, el `ComplexProperty` de
`Cliente.Direccion` solo mapea `Linea1`/`Linea2`) y `CategoriaProducto` (constructor de 4
parámetros, el `ComplexProperty` de `Producto.Categoria` solo mapea `Codigo`/`Nombre`) y
`Sector` (constructor de 2 parámetros, el `ComplexProperty` de `Cliente.Sector` solo mapea
`Nombre`). `Pais` y `UnidadMedida` NO están afectados: ya usaban un constructor privado más
un factory estático `Of(...)`, así que el parámetro `nombreCampo` vive fuera del
constructor que EF necesita enlazar — el patrón correcto que `DireccionCliente`,
`CategoriaProducto` y `Sector` deberían haber seguido.

**Files:**
- Modify: `src/OpenSource1.Core/ValueObjects/DireccionCliente.cs`
- Modify: `src/OpenSource1.Core/ValueObjects/CategoriaProducto.cs`
- Modify: `src/OpenSource1.Core/ValueObjects/Sector.cs`
- Modify: `src/OpenSource1.Infrastructure/Identity/DependencyInjection.cs` (validación JWT diferida)
- Modify: `tests/OpenSource1.SmokeTests/TestInfrastructure/PostgresTestFixture.cs` (aislamiento)
- Create: `tests/OpenSource1.SmokeTests/Infrastructure/ApplicationDbContextModelTests.cs`

- [ ] **Step 1: Añadir un constructor de arity menor que EF pueda enlazar**

Para cada VO afectado, añadir una sobrecarga SIN el parámetro `nombreCampo*`, con arity
distinta (no un valor por defecto — eso sería ambiguo con la sobrecarga existente), que
delega a la versión completa pasando `null`:

```csharp
// DireccionCliente
public DireccionCliente(string linea1, string? linea2) : this(linea1, linea2, null) { }
public DireccionCliente(string linea1, string? linea2, string? nombreCampoLinea1) { ... }

// CategoriaProducto
public CategoriaProducto(string codigo, string nombre) : this(codigo, nombre, null, null) { }
public CategoriaProducto(string codigo, string nombre, string? nombreCampoCodigo, string? nombreCampoNombre) { ... }

// Sector
public Sector(string nombre) : this(nombre, null) { }
public Sector(string nombre, string? nombreCampo) { ... }
```

El constructor sin `nombreCampo` queda sin valor por defecto en el segundo (o los dos
últimos) parámetro del constructor completo, para que la resolución de sobrecarga de C#
sea inequívoca por número de argumentos. EF Core prueba primero el constructor más
"goloso" que pueda enlazar completamente contra las propiedades mapeadas; al no poder
enlazar `nombreCampo*`, cae al de arity menor, que sí enlaza.

Ningún call site de producción cambia: todos pasan ya el número de argumentos con el
nombre de campo, así que siguen resolviendo al constructor completo.

- [ ] **Step 2: Test que hubiera detectado esto SIN Docker**

Ninguna verificación anterior (build, tests unitarios con Moq) ejercitó la construcción
real del modelo de EF, porque las escrituras se prueban con `IUnitOfWork` mockeado. Un
test que solo llame a `new DbContextOptionsBuilder<ApplicationDbContext>().UseNpgsql(cs)`
y fuerce el acceso al modelo (`.Model` o cualquier `IQueryable.ToQueryString()`) revienta
igual, **sin conexión ni Docker**, porque el fallo es de construcción del modelo, no de
I/O:

```csharp
[Fact]
public void ElModeloDeApplicationDbContext_SeConstruyeSinErrores()
{
    var options = new DbContextOptionsBuilder<ApplicationDbContext>()
        .UseNpgsql("Host=localhost;Database=validacion_de_modelo_solamente")
        .Options;

    using var contexto = new ApplicationDbContext(options);

    // Fuerza la construcción del modelo sin abrir conexión.
    var modelo = contexto.Model;

    Assert.NotNull(modelo);
    Assert.NotNull(modelo.FindEntityType(typeof(OpenSource1.Core.Entities.Cliente)));
    Assert.NotNull(modelo.FindEntityType(typeof(OpenSource1.Core.Entities.Producto)));
}
```

Este test debe quedar en la suite de forma permanente: es la red que faltó y que hubiera
detectado el defecto en el mismo lote que lo introdujo, sin depender de Docker.

- [ ] **Step 3: Validación de `Jwt:SigningKey` diferida, no eager**

`Identity/DependencyInjection.cs` valida `Jwt:SigningKey` con `configuration.GetSection("Jwt").Get<JwtOptions>()`
de forma síncrona durante el registro de servicios, **antes** de que
`WebApplicationFactory` (usado por los tests de integración) pueda inyectar su
configuración de prueba. Resultado: los 7 tests de `tests/OpenSource1.SmokeTests/Api/`
fallan con `JWT signing key must contain at least 32 characters` incluso con Docker
disponible y el placeholder real siendo un marcador de 21 caracteres.

Sustituir la validación eager por el patrón `ValidateOnStart()`, que corre al arrancar el
host — después de que toda la configuración, incluida la de test, esté resuelta:

```csharp
services.AddOptions<JwtOptions>()
    .Bind(configuration.GetSection("Jwt"))
    .Validate(o => !string.IsNullOrWhiteSpace(o.SigningKey) && o.SigningKey.Length >= 32,
        "JWT signing key must contain at least 32 characters.")
    .ValidateOnStart();
```

Verificar empíricamente contra Docker real que esto desbloquea los tests de `Api/` en
combinación con el Step 1 (ambos bloqueos son independientes y secuenciales: arreglar solo
uno no basta).

- [ ] **Step 4: Aislar `PostgresTestFixture` entre clases de test**

Nombre de contenedor y puerto fijos (`opensource1-tests-postgres`, `65432`) colisionan si
xUnit paraleliza clases que la usan — confirmado: ejecutar `Api.*` junto con
`DbSessionTests` sin agrupar produce `exit code 125` por colisión de nombre. Añadir una
`[CollectionDefinition("Postgres", DisableParallelization = true)]` y decorar con
`[Collection("Postgres")]` todas las clases que consumen el fixture
(`ClientesApiTests`, `ProductosApiTests`, `UsersApiTests`, `AppSettingsApiTests`,
`DbSessionTests`), de modo que se serialicen en vez de competir por el mismo contenedor.

- [ ] **Step 5: Verificación completa, con Docker real**

```bash
dotnet build test.slnx
dotnet test test.slnx --filter "FullyQualifiedName!~OpenSource1.SmokeTests.Api."
dotnet test test.slnx --filter "FullyQualifiedName~OpenSource1.SmokeTests.Api."
dotnet test test.slnx    # la suite completa, sin filtro, con la colección serializada
```

Los 7 tests de `Api/` deben quedar en VERDE — Docker ya está disponible. Si alguno sigue
en rojo, no se cierra el lote: hay que decir exactamente cuál y por qué.

- [ ] **Step 6: Commit**

Uno por cada defecto corregido, no un solo commit gigante.
### Task 1.6: API de transacción en `IUnitOfWork`

**Files:**
- Modify: `src/OpenSource1.Application/Data/UnitOfWork/IUnitOfWork.cs`
- Modify: `src/OpenSource1.Infrastructure/Data/UnitOfWork/UnitOfWork.cs`

**Interfaces:**
- Produces: `IUnitOfWork.BeginTransactionAsync`, `CommitAsync`, `RollbackAsync`,
  `HayTransaccionActiva`. Los consume el `TransactionBehavior` y los handlers de posteo
  de las Fases 4, 5 y 6.

- [ ] **Step 1: Ampliar el contrato**

```csharp
public interface IUnitOfWork
{
    IGenericRepository<TEntity> Repository<TEntity>() where TEntity : class, IAggregateRoot;
    Task<int> SaveChangesAsync(CancellationToken cancellationToken = default);

    bool HayTransaccionActiva { get; }
    Task<IAsyncDisposable> BeginTransactionAsync(CancellationToken cancellationToken = default);
    Task CommitAsync(CancellationToken cancellationToken = default);
    Task RollbackAsync(CancellationToken cancellationToken = default);
}
```

- [ ] **Step 2: Implementar delegando en `IDbSession`**

`UnitOfWork` recibe `IDbSession` por constructor. `BeginTransactionAsync` abre la
transacción en la sesión y **enrola el `DbContext`**:

```csharp
public async Task<IAsyncDisposable> BeginTransactionAsync(CancellationToken cancellationToken = default)
{
    var ambito = await session.BeginTransactionAsync(cancellationToken);

    if (dbContext.Database.CurrentTransaction is null && session.CurrentTransaction is not null)
    {
        await dbContext.Database.UseTransactionAsync(session.CurrentTransaction, cancellationToken);
    }

    return ambito;
}
```

`CommitAsync` llama a `SaveChangesAsync` y luego a `session.CommitAsync`.

- [ ] **Step 3: Verificar build**

```bash
dotnet build test.slnx
```

### Task 1.7: Behaviors de MediatR

**Files:**
- Create: `src/OpenSource1.Application/Common/Messaging/ICommand.cs`
- Create: `src/OpenSource1.Application/Common/Behaviors/ValidationBehavior.cs`
- Create: `src/OpenSource1.Application/Common/Behaviors/TransactionBehavior.cs`
- Create: `src/OpenSource1.Application/Common/Behaviors/LoggingBehavior.cs`
- Modify: `src/OpenSource1.Application/Services/DependencyInjection.cs:9-10`
- Test: `tests/OpenSource1.SmokeTests/Application/Behaviors/ValidationBehaviorTests.cs`

**Interfaces:**
- Produces: `ICommand<TResponse> : IRequest<TResponse>`, `IQuery<TResponse> : IRequest<TResponse>`.
  Todos los comandos de las Fases 2-6 implementan `ICommand<>` para obtener transacción
  automática.

- [ ] **Step 1: Marcadores**

```csharp
using MediatR;

namespace OpenSource1.Application.Common.Messaging;

/// <summary>Escribe. El TransactionBehavior lo envuelve en una transacción.</summary>
public interface ICommand<out TResponse> : IRequest<TResponse>;

/// <summary>Solo lee. No abre transacción.</summary>
public interface IQuery<out TResponse> : IRequest<TResponse>;
```

- [ ] **Step 2: `ValidationBehavior`**

Ejecuta todos los `IValidator<TRequest>` registrados. Si hay fallos, **no llama al
handler** y devuelve un `Result` fallido construido por reflexión sobre `TResponse`
(soporta `Result` y `Result<T>`). Si `TResponse` no es un `Result`, lanza
`ValidationException` (comportamiento de compatibilidad con los validadores de auth).

- [ ] **Step 3: `TransactionBehavior`**

```csharp
public async Task<TResponse> Handle(TRequest request, RequestHandlerDelegate<TResponse> next, CancellationToken cancellationToken)
{
    if (request is not ICommand<TResponse> || unitOfWork.HayTransaccionActiva)
    {
        return await next(cancellationToken);
    }

    await using var ambito = await unitOfWork.BeginTransactionAsync(cancellationToken);
    var respuesta = await next(cancellationToken);

    // Un Result fallido deshace: no se commitea un comando que reportó error.
    if (respuesta is Result resultado && resultado.EsFallo)
    {
        await unitOfWork.RollbackAsync(cancellationToken);
        return respuesta;
    }

    await unitOfWork.CommitAsync(cancellationToken);
    return respuesta;
}
```

Esta última condición es importante: hoy un handler puede devolver "no encontrado" tras
haber escrito algo, y se commitea igual.

- [ ] **Step 4: Registrar en orden**

```csharp
services.AddMediatR(configuration =>
{
    configuration.RegisterServicesFromAssembly(typeof(DependencyInjection).Assembly);
    configuration.AddOpenBehavior(typeof(LoggingBehavior<,>));
    configuration.AddOpenBehavior(typeof(ValidationBehavior<,>));
    configuration.AddOpenBehavior(typeof(TransactionBehavior<,>));
});
```

El orden importa: logging envuelve todo, la validación corre **antes** de abrir
transacción, y la transacción es lo más cercano al handler.

- [ ] **Step 5: Test del behavior de validación**

Con Moq: un `IValidator<T>` que falla → el handler **no** se invoca y el `Result`
devuelto es fallido con el código y campo esperados.

- [ ] **Step 6: Verificar**

```bash
dotnet build test.slnx
dotnet test test.slnx --filter "FullyQualifiedName~BehaviorTests"
```

### Task 1.8: Manejo global de errores

**Reordenada y ampliada por decisión del usuario.** Pasa a ejecutarse inmediatamente
después de la Task 1.3, no al final de la fase. Motivo: la Task 1.3 hace que un filtro
inválido (`?precio=abc`) lance `ArgumentException`, y sin `UseExceptionHandler` en el
pipeline eso sale como **500 desnudo sin cuerpo**. El bug anterior (devolver la tabla
completa) era peor en seguridad pero mejor en respuesta HTTP; dejarlo así sería una
regresión. El usuario dictaminó cubrirla ya.

La ampliación cubre además la misma clase de defecto que ya existía antes de este plan:
los factories de value objects (`Pais.Of` en `CreateClienteCommandHandler.cs:22`,
`UnidadMedida.Of` y `new CategoriaProducto(...)` en `CreateProductoCommandHandler.cs:20-21`)
lanzan `ArgumentException` sin que nadie la capture, produciendo 500 desnudos. Una
excepción de validación de dominio mapeada a 400 resuelve el conjunto, no solo el caso del
filtro.

**Files:**
- Create: `src/OpenSource1.Core/Common/ErroresDeDominioException.cs`
- Create: `src/OpenSource1.Api/Infrastructure/GlobalExceptionHandler.cs`
- Create: `src/OpenSource1.Api/Infrastructure/ResultExtensions.cs`
- Modify: `src/OpenSource1.Api/Program.cs:68` y `:102-128`
- Modify: los repositorios Dapper que hoy lanzan `ArgumentException` por filtro inválido
- Test: `tests/OpenSource1.SmokeTests/Core/Common/ErroresDeDominioExceptionTests.cs`

**Interfaces:**
- Produces: `ErroresDeDominioException(IReadOnlyList<Error> Errores)` con constructor de
  conveniencia `(Error error)`; `ResultExtensions.ToActionResult(this Result, ...)`.
  Los consumen la Task 1.5b y todas las fases siguientes.

- [ ] **Step 0: Excepción de validación de dominio**

```csharp
namespace OpenSource1.Core.Common;

/// <summary>
/// Válvula de escape para capas que no pueden devolver Result (constructores, factories de
/// value objects, validación dentro de una query ya en vuelo). El manejador global la
/// traduce a 400 con los errores por campo, en vez del 500 desnudo que produce hoy
/// cualquier ArgumentException del dominio.
/// </summary>
public sealed class ErroresDeDominioException : Exception
{
    public ErroresDeDominioException(IReadOnlyList<Error> errores)
        : base(string.Join("; ", errores.Select(e => e.Mensaje)))
    {
        if (errores.Count == 0)
        {
            throw new ArgumentException("Se requiere al menos un error.", nameof(errores));
        }

        Errores = errores;
    }

    public ErroresDeDominioException(Error error) : this([error]) { }

    public IReadOnlyList<Error> Errores { get; }
}
```

Los repositorios Dapper que la Task 1.3 dejó lanzando `ArgumentException` por filtro
inválido pasan a lanzar `ErroresDeDominioException` con el `Error` que ya produce
`AddExactFilter`. Es un cambio de una línea por call site y conserva el código de error
`filtro.valor_invalido` y el nombre del campo.

- [ ] **Step 1: `IExceptionHandler`**

Registra la excepción con `ILogger` y escribe `ProblemDetails` con
`status 500`, `title "Error interno del servidor."` y `traceId`. En Development incluye
`detail` con el mensaje; en producción no.

- [ ] **Step 2: Enchufarlo en el pipeline**

```csharp
builder.Services.AddExceptionHandler<GlobalExceptionHandler>();
builder.Services.AddProblemDetails();
...
app.UseExceptionHandler();   // FALTA HOY: sin esto, AddProblemDetails no hace nada ante excepciones
```

`app.UseExceptionHandler()` va **antes** de `UseStatusCodePages`.

- [ ] **Step 2b: Mapear `ErroresDeDominioException` a 400**

El `GlobalExceptionHandler` distingue dos casos antes del genérico:

```csharp
if (exception is ErroresDeDominioException dominio)
{
    var problema = new ValidationProblemDetails(
        dominio.Errores
            .GroupBy(e => e.Campo ?? string.Empty)
            .ToDictionary(g => g.Key, g => g.Select(e => e.Mensaje).ToArray()))
    {
        Status = StatusCodes.Status400BadRequest,
        Title = "Los datos enviados no son válidos.",
    };
    // ... escribir problema, devolver true
}

if (exception is DbUpdateConcurrencyException)   // Task 1.9
{
    // 409, "El registro fue modificado por otro usuario."
}
```

**Verificación obligatoria de la regresión:** un test que compruebe que un filtro inválido
produce **400 con cuerpo que nombra el campo**, no 500 y no la tabla completa. Si no puede
ser un test de API por falta de Docker, debe ser al menos un test directo del manejador
con una `ErroresDeDominioException` construida a mano, más una nota explícita en el informe
de que el camino HTTP completo quedó sin verificar.

- [ ] **Step 3: `ResultExtensions`**

```csharp
public static IActionResult ToActionResult(this Result resultado, ...)
```

Mapeo por sufijo del código de error: `*.no_encontrado` → 404, `*.conflicto` → 409,
`*.bloqueado` → 422, resto → 400. El cuerpo es `ValidationProblemDetails` con los errores
agrupados por `Campo`.

- [ ] **Step 4: Verificar**

```bash
dotnet build test.slnx
```

### Task 1.9: Concurrencia optimista con `xmin`

**Files:**
- Modify: `src/OpenSource1.Infrastructure/Data/ApplicationDbContext.cs`
- Modify: `src/OpenSource1.Api/Infrastructure/GlobalExceptionHandler.cs`

- [ ] **Step 1: Declarar el token en cada entidad**

PostgreSQL expone `xmin` como columna de sistema: **no requiere migración**.

```csharp
// dentro de cada modelBuilder.Entity<T>(entity => { ... })
entity.Property<uint>("xmin").HasColumnName("xmin").IsRowVersion();
```

Aplicar a `AppSetting`, `Entrada`, `Cliente` y `Producto`.

- [ ] **Step 2: Traducir el conflicto a 409**

En `GlobalExceptionHandler`, capturar `DbUpdateConcurrencyException` y devolver
`ProblemDetails` con status 409 y título "El registro fue modificado por otro usuario.".

- [ ] **Step 3: Verificar que no se generó migración espuria**

```bash
dotnet build test.slnx
dotnet ef migrations list --project src/OpenSource1.Infrastructure --startup-project src/OpenSource1.Api
```
Esperado: sin migración nueva pendiente por este cambio.

### Task 1.10: Soft delete

**Files:**
- Create: `src/OpenSource1.Core/Abstractions/ISoftDeletable.cs`
- Modify: `src/OpenSource1.Core/Abstractions/AuditableEntity.cs`
- Modify: `src/OpenSource1.Infrastructure/Data/ApplicationDbContext.cs`
- Modify: `src/OpenSource1.Infrastructure/Data/UnitOfWork/UnitOfWork.cs`
- Create: migración `AddSoftDelete`

- [ ] **Step 1: Interfaz y propiedades**

```csharp
namespace OpenSource1.Core.Abstractions;

public interface ISoftDeletable
{
    bool IsDeleted { get; set; }
    DateTimeOffset? DeletedAtUtc { get; set; }
    string? DeletedBy { get; set; }
}
```

`AuditableEntity<TKey>` pasa a implementarla.

- [ ] **Step 2: Filtro global y columnas**

```csharp
entity.Property(x => x.IsDeleted).HasDefaultValue(false);
entity.Property(x => x.DeletedBy).HasMaxLength(100);
entity.HasQueryFilter(x => !x.IsDeleted);
```

- [ ] **Step 3: Interceptar el borrado en `UnitOfWork`**

En `ApplyAuditValues`, convertir `EntityState.Deleted` en `Modified` marcando
`IsDeleted = true`, `DeletedAtUtc`, `DeletedBy`.

- [ ] **Step 4: Los repositorios Dapper filtran explícitamente**

Añadir `AND "IsDeleted" = false` a las 8 queries. El filtro global de EF **no** aplica a
Dapper.

- [ ] **Step 5: Migración**

```bash
dotnet ef migrations add AddSoftDelete --project src/OpenSource1.Infrastructure --startup-project src/OpenSource1.Api --output-dir Data/Migrations/Application
```

- [ ] **Step 6: Verificar**

```bash
dotnet build test.slnx && dotnet test test.slnx --filter "FullyQualifiedName~Handlers"
```

### Task 1.11: Índices

**Files:**
- Modify: `src/OpenSource1.Infrastructure/Data/ApplicationDbContext.cs`
- Create: migración `AddIndicesBase`

- [ ] **Step 1: Índices declarativos**

```csharp
entity.HasIndex(x => x.CreatedAtUtc).HasDatabaseName("IX_<Tabla>_CreatedAtUtc");
```
en las 4 entidades (es la columna de `ORDER BY` de **todos** los listados y hoy no tiene
índice), más un único en `Clientes.Email`.

- [ ] **Step 2: `pg_trgm` y GIN en SQL crudo**

Los `ILIKE '%term%'` de `FilterExpressionBuilder.cs:27` llevan wildcard inicial: ningún
B-tree sirve. En la migración, `migrationBuilder.Sql(...)`:

```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE INDEX IF NOT EXISTS "IX_Clientes_Nombre_trgm"  ON "Clientes" USING GIN ("Nombre"  gin_trgm_ops);
CREATE INDEX IF NOT EXISTS "IX_Clientes_Apellido_trgm" ON "Clientes" USING GIN ("Apellido" gin_trgm_ops);
CREATE INDEX IF NOT EXISTS "IX_Clientes_Email_trgm"   ON "Clientes" USING GIN ("Email"   gin_trgm_ops);
CREATE INDEX IF NOT EXISTS "IX_Productos_Nombre_trgm" ON "Productos" USING GIN ("Nombre" gin_trgm_ops);
```

Con su `Down` correspondiente (`DROP INDEX`).

- [ ] **Step 3: Verificar**

```bash
dotnet build test.slnx
```

### Task 1.12: Permisos por recurso

**Files:**
- Create: `src/OpenSource1.Application/Security/Permisos.cs`
- Create: `src/OpenSource1.Application/Security/PermisosPorRol.cs`
- Create: `src/OpenSource1.Infrastructure/Identity/PermissionPolicyProvider.cs`
- Create: `src/OpenSource1.Infrastructure/Identity/PermissionAuthorizationHandler.cs`
- Modify: `src/OpenSource1.Infrastructure/Identity/DependencyInjection.cs:70-80`
- Modify: `src/OpenSource1.Infrastructure/Services/Auth/AuthService.cs` (emitir claims)
- Test: `tests/OpenSource1.SmokeTests/Application/Security/PermisosTests.cs`

- [ ] **Step 1: Catálogo de permisos**

Constantes `<modulo>.<recurso>.<accion>`, cubriendo los módulos existentes y los que
llegan en las fases siguientes: `maestros.socio.consultar/agregar/modificar/eliminar`,
`maestros.producto.*`, `inventario.almacen.*`, `inventario.diario.capturar/postear`,
`contabilidad.cuenta.*`, `contabilidad.setup.*`, `facturacion.factura.capturar/emitir`,
`facturacion.cobro.registrar/aplicar`.

- [ ] **Step 2: Mapeo rol → permisos**

`Administrador` → todos. `Supervisor` → consultar + modificar + postear.
`Ejecutor` → consultar + agregar + capturar.

Es el equivalente por recurso de la matriz actual de `CanConsult`/`CanAdd`/`CanModify`/`CanDelete`.

- [ ] **Step 3: `PermissionPolicyProvider`**

Crea políticas al vuelo para cualquier nombre que empiece por `permiso:`, evaluadas contra
los claims del JWT. Las 4 políticas existentes se **conservan como alias** para no romper
los controllers vigentes (`ClientesController.cs:18,38,48,70,93`).

- [ ] **Step 4: Emitir los claims en el token**

`AuthService` añade un claim `permiso` por cada permiso del rol del usuario.

- [ ] **Step 5: Test**

Que cada rol tenga exactamente el conjunto de permisos esperado y que
`Administrador` los tenga todos.

- [ ] **Step 6: Verificar**

```bash
dotnet build test.slnx && dotnet test test.slnx --filter "FullyQualifiedName~PermisosTests"
```

---

## Verificación de cierre de las Fases 0 y 1

- [ ] `dotnet build test.slnx` → 0 errores, 0 warnings NU1903
- [ ] `dotnet test test.slnx --filter "FullyQualifiedName!~Api"` → todos los unitarios verdes
- [ ] Tests de integración: **no ejecutables sin Docker**. Reportar como no verificados.
- [ ] `git ls-files | grep appsettings.json` → vacío
- [ ] Ningún `IDbConnectionFactory` restante: `grep -rn IDbConnectionFactory src/` → vacío
