name: PR to Main - Enforce QA and Validate

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

jobs:
  enforce-qa-source:
    name: Enforce source branch qa
    if: ${{ !github.event.pull_request.draft }}
    runs-on: ubuntu-latest
    steps:
      - name: Fail if PR source is not qa
        run: |
          if [ "${{ github.head_ref }}" != "qa" ]; then
            echo "Only PRs from 'qa' to 'main' are allowed. Current source: '${{ github.head_ref }}'."
            exit 1
          fi

  validate:
    name: Restore, Build and Test
    if: ${{ !github.event.pull_request.draft }}
    needs: enforce-qa-source
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore "$GITHUB_WORKSPACE/test.slnx"

      - name: Build
        run: dotnet build "$GITHUB_WORKSPACE/test.slnx" --configuration Release --no-restore

      - name: Test
        run: dotnet test "$GITHUB_WORKSPACE/test.slnx" --configuration Release --no-build
