@using OpenSource1.Blazor.Components.Pages

<div class="space-y-4">
    <div>
        <label class="block text-sm font-semibold text-slate-700 mb-1.5">
            Título <span class="text-red-500">*</span>
        </label>
        <InputText class="block w-full rounded-lg border border-slate-300 bg-slate-50 px-3 py-2 text-sm text-slate-800 placeholder-slate-400 shadow-sm focus:border-violet-400 focus:outline-none focus:ring-2 focus:ring-violet-400/20"
                   @bind-Value="Model.Titulo"
                   placeholder="Título de la entrada" />
        <ValidationMessage For="() => Model.Titulo" />
    </div>

    <div>
        <label class="block text-sm font-semibold text-slate-700 mb-1.5">Descripción</label>
        <InputTextArea class="block w-full rounded-lg border border-slate-300 bg-slate-50 px-3 py-2 text-sm text-slate-800 placeholder-slate-400 shadow-sm focus:border-violet-400 focus:outline-none focus:ring-2 focus:ring-violet-400/20 resize-none"
                       @bind-Value="Model.Descripcion"
                       rows="3"
                       placeholder="Descripción opcional" />
        <ValidationMessage For="() => Model.Descripcion" />
    </div>

    <div>
        <label class="block text-sm font-semibold text-slate-700 mb-1.5">
            Tipo <span class="text-red-500">*</span>
        </label>
        <InputText class="block w-full rounded-lg border border-slate-300 bg-slate-50 px-3 py-2 text-sm text-slate-800 placeholder-slate-400 shadow-sm focus:border-violet-400 focus:outline-none focus:ring-2 focus:ring-violet-400/20"
                   @bind-Value="Model.Tipo"
                   placeholder="Ej. Contrato, Solicitud, Reporte" />
        <ValidationMessage For="() => Model.Tipo" />
    </div>

    <div>
        <label class="block text-sm font-semibold text-slate-700 mb-1.5">
            Estado <span class="text-red-500">*</span>
        </label>
        <InputText class="block w-full rounded-lg border border-slate-300 bg-slate-50 px-3 py-2 text-sm text-slate-800 placeholder-slate-400 shadow-sm focus:border-violet-400 focus:outline-none focus:ring-2 focus:ring-violet-400/20"
                   @bind-Value="Model.Estado"
                   placeholder="Ej. Activo, Pendiente, Cerrado" />
        <ValidationMessage For="() => Model.Estado" />
    </div>
</div>

@code {
    [Parameter, EditorRequired] public Entradas.EntradaForm Model { get; set; } = default!;
}
