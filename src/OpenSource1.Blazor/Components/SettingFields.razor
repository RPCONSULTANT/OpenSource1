@using OpenSource1.Blazor.Components.Pages

@*
    SettingFields — campos reutilizables para crear / modificar un AppSetting.

    Requiere el parámetro Prefix con el nombre exacto de la propiedad
    [SupplyParameterFromForm] del componente padre:
      - Agregar:   Prefix="SaveInput"
      - Modificar: Prefix="UpdateInput"

    El FormDataMapper de Blazor SSR usa el nombre del parámetro como prefijo.
*@

<div class="space-y-4">
    <div>
        <label class="block text-sm font-medium text-slate-700 mb-1" for="settingKey">
            Clave <span class="text-red-500">*</span>
        </label>
        <input id="settingKey"
               type="text"
               name="@($"{Prefix}.Key")"
               value="@Model.Key"
               placeholder="ej. App:Feature:Enabled"
               class="block w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm text-slate-800 placeholder-slate-400 shadow-sm focus:border-brand-600 focus:outline-none focus:ring-2 focus:ring-brand-600/20" />
        <ValidationMessage For="() => Model.Key" />
    </div>

    <div>
        <label class="block text-sm font-medium text-slate-700 mb-1" for="settingValue">
            Valor <span class="text-red-500">*</span>
        </label>
        <input id="settingValue"
               type="text"
               name="@($"{Prefix}.Value")"
               value="@Model.Value"
               placeholder="ej. true"
               class="block w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm text-slate-800 placeholder-slate-400 shadow-sm focus:border-brand-600 focus:outline-none focus:ring-2 focus:ring-brand-600/20" />
        <ValidationMessage For="() => Model.Value" />
    </div>

    <div>
        <label class="block text-sm font-medium text-slate-700 mb-1" for="settingDescription">
            Descripción
        </label>
        <input id="settingDescription"
               type="text"
               name="@($"{Prefix}.Description")"
               value="@Model.Description"
               placeholder="Descripción opcional"
               class="block w-full rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm text-slate-800 placeholder-slate-400 shadow-sm focus:border-brand-600 focus:outline-none focus:ring-2 focus:ring-brand-600/20" />
        <ValidationMessage For="() => Model.Description" />
    </div>
</div>

@code {
    [Parameter, EditorRequired] public AppSettings.SettingForm Model { get; set; } = default!;
    /// <summary>
    /// Nombre exacto del parámetro [SupplyParameterFromForm] en el componente padre.
    /// Ej: "SaveInput" o "UpdateInput"
    /// </summary>
    [Parameter, EditorRequired] public string Prefix { get; set; } = default!;
}
